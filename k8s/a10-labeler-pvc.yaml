apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: perfseer-v3-a10-labels
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 700Gi
  storageClassName: rook-cephfs
