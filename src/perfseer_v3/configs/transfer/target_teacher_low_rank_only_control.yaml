extends: ../train_hardware_teacher/v3_teacher.yaml
run:
  run_id: perfseer_v3_target_teacher_low_rank_only_control
model:
  capacity_candidate: T1_target_low_rank_only
  adapter_rank: 32
  adapter_policy: low_rank_only
training:
  stage: target_teacher_adapter
  pretrain_epochs: 0
  initialization: a10_base_teacher_artifact
  epochs: 300
  batch_size: 32
  learning_rate: 0.001
  adapter_regularization_weight: 0.0001
  l2_sp_weight: 0.0001
  residual_epsilon: 0.000001
  utilization_clip: 0.005
  validation_interval_epochs: 1
  early_stopping_patience_checks: 30
  early_stopping_min_delta: 0.0001
