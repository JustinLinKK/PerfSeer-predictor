extends: ../train_hardware_teacher/v3_teacher.yaml
run:
  run_id: perfseer_v3_target_teacher_adapter_heads_ablation
model:
  capacity_candidate: T1_target_rank32_heads
  adapter_rank: 32
  adapter_policy: film_low_rank_heads
training:
  stage: target_teacher_adapter
  pretrain_epochs: 0
  initialization: a10_base_teacher_artifact
  enabled: false
  validation_interval_epochs: 1
  early_stopping_patience_checks: 30
  early_stopping_min_delta: 0.0001
