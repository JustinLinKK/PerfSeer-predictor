# Nautilus E2E Label Sampling Smoke

- Date: 2026-06-11.

- Run ID: `smoke6-20260611173439`.

- Namespace: `ecepxie`.

- Persistent Volume Claim: `test-pvc`.

- Goal: verify one script can apply four different Nautilus GPU jobs, run label sampling in parallel, and verify labels.

- GPU sampling image: `pytorch/pytorch:2.3.0-cuda11.8-cudnn8-devel`.

- Verifier image: `python:3.11-slim`.

- Sample model: `nautilus_smoke_mlp`.

- Sampling process: one warmup epoch, then one profiled epoch.

- GPU jobs completed: `a10`, `a40`, `a100`, `v100`.

- Kubernetes job count: 5 complete, including final verifier job.

- GPU pod nodes: `hcc-nrp-shor-c6009.unl.edu`, `emporia.gp-argo.greatplains.net`, `bak-hpc1.csub.edu`, `chi-dgx-node03.csuchico.edu`.

- Verifier pod node: `tuskegee-ps.sox.net`.

- Per-GPU verifier output: `bad_rows=0`, `files=1`, `ok_rows=1`, `rows=1`.

- Cross-GPU verifier output: `bad_rows=0`, `files=4`, `ok_rows=4`, `rows=4`.

- Distinct GPU count: 4.

- Distinct GPU names: `NVIDIA A10`, `NVIDIA A100-PCIE-40GB`, `NVIDIA A40`, `Tesla V100-SXM2-16GB`.

- Label sampling logs:

```text
nautilus_smoke_mlp::fp32_ieee: ok
{"bad_rows": 0, "files": 1, "ok_rows": 1, "rows": 1}
```

- Final verifier logs:

```text
{"bad_rows": 0, "files": 4, "ok_rows": 4, "rows": 4}
{"gpu_count": 4, "gpu_names": ["NVIDIA A10", "NVIDIA A100-PCIE-40GB", "NVIDIA A40", "Tesla V100-SXM2-16GB"], "rows": 4}
```

- Conclusion: end-to-end Nautilus workflow verified.

- Cleanup: Kubernetes jobs and ConfigMap deleted after verification.

- Cleanup: failed smoke attempt records were deleted.
